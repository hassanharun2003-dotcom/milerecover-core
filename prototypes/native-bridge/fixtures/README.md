# Synthetic fixtures — Prototype C

Synthetic data only (grid origin 10.0, 10.0). No real locations.

```bash
cd prototypes/native-bridge
node fixtures/generate-synthetic-events.mjs --out fixtures/synthetic-sample.json
```

Used by manual validation and contract tests.
