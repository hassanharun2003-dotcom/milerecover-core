# Artwork Inventory — From Approved Design Board

**Policy:** Preserve usable original artwork via direct extraction from
`MileRecover-LOCKED-DESIGN.png`. Do not replace with crude placeholder geometry.
Do not invent unrelated artwork.

**Binary status:** PENDING — no extraction performed.

| Asset ID | Description | Extraction status | Notes |
|---|---|---|---|
| `logo-shield-road` | White shield with stylized road curving upwards | `PENDING_BINARY` → if unclean: `REQUIRES_VECTOR_RECREATION` | Splash + Welcome |
| `shield-protection` | Protection / “You’re protected” shield artwork | `PENDING_BINARY` → if unclean: `REQUIRES_VECTOR_RECREATION` | Home hero / Protection Center |
| `illustration-missing-drives` | Car on road with green hills and location pins | `PENDING_BINARY` → if unclean: `REQUIRES_VECTOR_RECREATION` | Missing Drives header |
| `map-route-polyline` | Green stylized route line-map preview | `PENDING_BINARY` → if unclean: `REQUIRES_VECTOR_RECREATION` | Review TripCard evidence |
| `icon-benefit-dollar` | Find missing miles | `PENDING_BINARY` / `REQUIRES_VECTOR_RECREATION` | Marketing + benefits |
| `icon-benefit-shield` | Proof you can trust | `PENDING_BINARY` / `REQUIRES_VECTOR_RECREATION` | |
| `icon-benefit-bolt` | Automatic & effortless | `PENDING_BINARY` / `REQUIRES_VECTOR_RECREATION` | |
| `country-flag-us` | US flag in circular treatment | Use ISO flag strategy — see `COUNTRY_FLAGS.md` | Do not hand-invent |

## Recreation rule

If an asset cannot be cleanly extracted from the raster reference, mark:

`REQUIRES_VECTOR_RECREATION`

Then recreate **faithfully in Figma** using the original crop as visual
reference — not a new concept.
