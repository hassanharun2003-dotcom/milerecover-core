# Final product lock evidence (`0.1.8-product-lock.1`)

## Automated (this environment)

| Artifact | Notes |
|---|---|
| Domain tests | 92 passed (`packages/domain`) |
| Mobile tests | 89 passed (`apps/mobile-expo`) |
| Typecheck | `tsc --noEmit` green (mobile + domain) |
| check:all | green |
| Screen copy evidence | `docs/assets/ui-evidence/*.json` regenerated by `screenEvidence.test.tsx` |
| Product lock doc | `docs/product/FINAL_PRODUCT_LOCK.md` |

## Device / visual checklist

Capture on a physical Android / iPhone after installing the preview build:

1. Onboarding — Welcome → Purpose → Country/Units/Rate → Protect → Ready
2. Home — greeting, protection, period, next, recovery alert, recent, Add a drive
3. Review — map preview, Work/Personal/Not sure/Edit/Undo
4. Proof — Week/Month/Quarter/Year/YTD + Reports + Readiness
5. Profile — locked section groups
6. Vehicles — searchable make/model + primary default
7. Trip details — route replay panel
8. Dark mode system appearance
9. Offline + force-close reopen

Store screenshots in this folder as `01-….png` … when captured.
